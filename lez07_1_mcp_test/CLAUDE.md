Use the company-directory MCP server.

Read all company users and department summaries.

Then create a static Bootstrap 5 dashboard.

The generated website must not call the MCP server at runtime.

Use the MCP data only to initialize local JavaScript arrays.

Create:
- index.html
- styles.css
- app.js

Include:
- summary cards
- users table
- department cards
- manager/direct reports section
- search box
